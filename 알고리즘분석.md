## 파이썬 단어 게임 알고리즘 분석

### word_module 모듈화
#### wordLoad() 메서드 구현
- ./data/word.txt 파일을 읽는다.
- 단어를 읽어 공백, 뉴라인을 제거하고 단어 리스트를 만든다.
- 단어 리스트를 반환한다.

#### _scorePrint() 메서드 구현
- 모듈 내부에서만 사용하므로 private 함수로 구현한다.
- 총 맞춘 개수 및 걸린 시간을 파리미터로 받는다.
- 결과를 화면에 출력한다.

#### _saveResult() 메서드 구현
- 모듈 내부에서만 사용하므로 private 함수로 구현한다.
- 총 맞춘 개수 및 걸린 시간을 파리미터로 받는다.
- 결과를 csv 파일에 append로 이어 붙인다.

#### gameRun() 메서드 구현
- count를 통해 총 5회 실행되도록 설정한다.
- 게임 시작과 끝에 스톱워치를 통해 총 걸린 시간을 구한다.
- 제시어와 입력한 정답이 일치하면 스코어가 1점씩 추가된다.
- _scorePrint()를 호출하여 결과를 화면에 출력한다.
- _saveResult()를 호출하여 결과를 csv 파일에 저장한다.